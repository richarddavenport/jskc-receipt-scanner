{
  "name": "js-kc-receipt-scanner",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "firebase serve",
    "firebase": "firebase",
    "firebase-bolt": "firebase-bolt",
    "deploy": "firebase deploy",
    "deploy:functions": "npm run deploy -- --only functions",
    "deploy:hosting": "npm run deploy -- --only hosting",
    "deploy:database": "npm run deploy -- --only database"
  },
  "devDependencies": {
    "firebase-bolt": "^0.8.2",
    "firebase-tools": "^3.10.1"
  }
}
